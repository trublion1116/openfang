# OpenFang 本地部署指南（Windows + 智谱 GLM）

> 环境信息：Windows 11、Rust 1.95.0 (MSVC toolchain)、Git Bash

## 一、项目简介

[OpenFang](https://github.com/RightNow-AI/openfang) 是一个用 Rust 编写的开源 Agent 操作系统（v0.6.4），包含 14 个 crate、约 13.7 万行代码。它不是一个聊天机器人框架，而是一个可以让 AI Agent 7×24 小时自主运行的完整 OS。

核心能力：知识图谱构建、目标监控、线索生成、社交媒体管理、Web Dashboard。

## 二、环境准备

### 1. 克隆仓库

```bash
git clone https://github.com/RightNow-AI/openfang.git
cd openfang
```

### 2. 确认 Rust 工具链

项目要求 Rust 1.75+，检查当前版本：

```bash
rustc --version
# rustc 1.95.0 — 满足要求

rustup show
# 确认是 x86_64-pc-windows-msvc 工具链
```

### 3. 构建

```bash
cargo build --workspace
```

首次编译耗时约 4 分钟（debug profile）。编译完成后生成 CLI 二进制文件：

```
target/debug/openfang.exe
```

## 三、配置（踩坑重点）

### 坑 1：需要手动创建配置目录和文件

项目不会自动生成配置，需要手动创建 `~/.openfang/config.toml`：

```bash
mkdir -p ~/.openfang
```

### 坑 2：默认监听端口是 50051，不是文档说的 4200

`config.toml` 里如果不指定 `api_listen`，默认监听 `127.0.0.1:50051`，但文档和示例都写的是 4200。启动时需要通过环境变量覆盖：

```bash
export OPENFANG_LISTEN="127.0.0.1:4200"
```

或者在 `config.toml` 里显式指定（但字段名是 `api_listen`，不是 `OPENFANG_LISTEN`）。

### 坑 3：智谱 GLM 的 base_url 拼写

OpenFang 的 OpenAI 驱动会拼接 `{base_url}/chat/completions`，所以 `base_url` 应该设置为：

```
https://open.bigmodel.cn/api/paas/v4
```

**不要**末尾加 `/`，否则会变成 `v4//chat/completions`。

### 完整 config.toml

```toml
# ~/.openfang/config.toml

[default_model]
provider = "openai"                                    # 使用 OpenAI 兼容驱动
model = "glm-5.1"                                     # 智谱模型名称
api_key_env = "OPENAI_API_KEY"                        # 从这个环境变量读取 API Key
base_url = "https://open.bigmodel.cn/api/paas/v4"    # 智谱 API 地址（不带尾部斜杠）

[memory]
decay_rate = 0.05
```

## 四、启动

### 坑 4：可能有残留进程占用端口

如果之前启动过，会报错 `Daemon already running`。需要先杀掉：

```bash
tasklist | grep -i openfang
taskkill //PID <pid> //F    # 注意 Git Bash 里用双斜杠 //
```

### 正确的启动命令

```bash
export OPENAI_API_KEY="你的智谱API Key"
export OPENFANG_LISTEN="127.0.0.1:4200"

./target/debug/openfang.exe start
```

启动成功后会看到：

```
>> OpenFang Agent OS
   The open-source agent operating system

✔ Kernel booted (openai/glm-5.1)
✔ 200 models available
✔ 1 agent(s) loaded

API:         http://127.0.0.1:4200
Dashboard:   http://127.0.0.1:4200/
Provider:    openai
Model:       glm-5.1
```

### 验证

```bash
curl -s http://127.0.0.1:4200/api/health
# {"status":"ok","version":"0.6.4"}
```

## 五、可以忽略的警告

启动后会出现以下 WARN，均不影响使用：

| 警告 | 原因 |
|------|------|
| `Local provider offline: ollama` | 本地没装 Ollama，不影响 |
| `Local provider offline: vllm` | 本地没跑 vLLM，不影响 |
| `Local provider offline: lmstudio` | 本地没跑 LM Studio，不影响 |
| `Embedding driver configured to send data to external API` | 向量嵌入会发到 OpenAI API，如果你的 Key 只有智谱的，嵌入功能可能不可用，但核心对话不受影响 |
| `imap-proto v0.10.2 future incompatibility` | 编译期 warning，不影响运行 |

## 六、踩坑汇总

| # | 坑 | 解决方案 |
|---|----|---------|
| 1 | 配置文件不会自动生成 | 手动 `mkdir ~/.openfang` 并创建 `config.toml` |
| 2 | 默认端口 50051 ≠ 文档写的 4200 | 设置 `OPENFANG_LISTEN="127.0.0.1:4200"` |
| 3 | 残留进程导致启动失败 | `taskkill //PID <pid> //F` 先杀掉 |
| 4 | 智谱 base_url 末尾斜杠 | 设为 `https://open.bigmodel.cn/api/paas/v4`（不带 `/`） |
| 5 | Git Bash 下 taskkill 语法 | 用 `//PID` 和 `//F`（双斜杠） |
| 6 | 环境变量名是 `OPENAI_API_KEY` | 即使不用 OpenAI，也要用这个名字，因为 `config.toml` 里 `api_key_env` 指向它 |

## 七、快速启动脚本

把以下内容保存为 `start.sh`：

```bash
#!/bin/bash
cd "$(dirname "$0")"

export OPENAI_API_KEY="你的智谱API Key"
export OPENFANG_LISTEN="127.0.0.1:4200"

# 杀掉残留进程
pids=$(tasklist | grep -i openfang | awk '{print $2}')
if [ -n "$pids" ]; then
    echo "Killing existing openfang process(es)..."
    for pid in $pids; do
        taskkill //PID "$pid" //F 2>/dev/null
    done
    sleep 2
fi

./target/debug/openfang.exe start
```

下次只需 `bash start.sh` 即可一键启动。
