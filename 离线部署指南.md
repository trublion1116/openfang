# OpenFang 离线 Docker 部署指南

> 目标：在无外网环境的 Linux 机器上部署 OpenFang Agent OS
> 镜像：`openfang-0.6.4-offline`（压缩包 56MB，解压后 147MB）

## 一、前提条件

- Linux 服务器已安装 Docker（无需 docker-compose）
- 服务器可通过内网访问智谱 API 地址 `open.bigmodel.cn`（如果完全断网则无法调用 LLM）
- 已将 `openfang-0.6.4-offline.tar.gz` 传到服务器上（U盘、scp 等方式）

## 二、加载镜像

```bash
gunzip -c openfang-0.6.4-offline.tar.gz | docker load
```

验证镜像已加载：

```bash
docker images | grep openfang
# openfang   0.6.4-offline   xxxxx   147MB
```

## 三、启动服务

```bash
docker run -d \
  --name openfang \
  -p 4200:4200 \
  -v openfang-data:/data \
  -e OPENAI_API_KEY="你的智谱API Key" \
  --restart unless-stopped \
  openfang:0.6.4-offline
```

参数说明：

| 参数 | 作用 |
|------|------|
| `-d` | 后台运行 |
| `--name openfang` | 容器命名为 openfang |
| `-p 4200:4200` | 映射端口，Dashboard/API 通过 4200 访问 |
| `-v openfang-data:/data` | 持久化数据目录（Agent、会话、数据库等） |
| `-e OPENAI_API_KEY=...` | 智谱 GLM 的 API Key（必填） |
| `--restart unless-stopped` | 异常退出或机器重启后自动恢复 |

## 四、验证

### 4.1 检查容器状态

```bash
docker ps | grep openfang
# STATUS 列应显示 Up
```

### 4.2 查看启动日志

```bash
docker logs openfang 2>&1 | head -30
```

正常输出应包含：

```
✔ Kernel booted (openai/glm-5.1)
✔ 200 models available
✔ 1 agent(s) loaded
API:         http://0.0.0.0:4200
Dashboard:   http://0.0.0.0:4200/
```

### 4.3 健康检查

```bash
curl -s http://localhost:4200/api/health
# 期望输出：{"status":"ok","version":"0.6.4"}
```

### 4.4 测试 LLM 对话

```bash
# 获取 Agent ID
AGENT_ID=$(curl -s http://localhost:4200/api/agents | python3 -c "import sys,json; print(json.load(sys.stdin)[0]['id'])")

# 发送消息（触发真实 LLM 调用）
curl -s -X POST "http://localhost:4200/api/agents/$AGENT_ID/message" \
  -H "Content-Type: application/json" \
  -d '{"message": "你好，请用一句话介绍你自己"}'
```

如果返回包含 LLM 回复内容，说明智谱 GLM 连通正常。

### 4.5 打开 Dashboard

浏览器访问：

```
http://<服务器IP>:4200/
```

## 五、常用运维命令

```bash
# 查看实时日志
docker logs -f openfang

# 重启服务
docker restart openfang

# 停止服务
docker stop openfang

# 启动已有容器
docker start openfang

# 删除容器（数据在 volume 中不会丢失）
docker rm -f openfang

# 查看持久化数据
docker volume inspect openfang-data
```

## 六、更新镜像

当需要更新到新版本时：

```bash
# 1. 传入新版本镜像包并加载
docker load -i openfang-x.x.x-offline.tar.gz

# 2. 停掉旧容器
docker stop openfang && docker rm openfang

# 3. 用新镜像启动（数据卷自动复用）
docker run -d \
  --name openfang \
  -p 4200:4200 \
  -v openfang-data:/data \
  -e OPENAI_API_KEY="你的智谱API Key" \
  --restart unless-stopped \
  openfang:x.x.x-offline
```

## 七、切换 LLM 提供商

镜像默认配置为智谱 GLM。如需更换 LLM 提供商或 API 地址，**挂载自定义配置文件**即可覆盖：

```bash
-v /opt/openfang/config.toml:/data/config.toml
```

> 仅更换 API Key 时不需要挂载文件，直接通过 `-e` 传入环境变量即可。但 `provider`、`model`、`base_url` 必须通过配置文件修改。

### 7.1 配置速查表

| 提供商 | provider | base_url | 需要 API Key |
|--------|----------|----------|-------------|
| 智谱 GLM | `openai` | `https://open.bigmodel.cn/api/paas/v4` | 是 |
| DeepSeek | `openai` | `https://api.deepseek.com/v1` | 是 |
| OpenAI | `openai` | 不填（默认） | 是 |
| Groq | `groq` | 不填 | 是 |
| Anthropic | `anthropic` | 不填 | 是 |
| Gemini | `gemini` | 不填 | 是 |
| Ollama（本地） | `ollama` | 不填 | 否 |
| vLLM（本地） | `openai` | `http://localhost:8000/v1` | 否 |

### 7.2 示例：切换到 DeepSeek

```bash
cat > /opt/openfang/config.toml << 'EOF'
[default_model]
provider = "openai"
model = "deepseek-chat"
api_key_env = "DEEPSEEK_API_KEY"
base_url = "https://api.deepseek.com/v1"

[memory]
decay_rate = 0.05
EOF

docker run -d \
  --name openfang \
  -p 4200:4200 \
  -v openfang-data:/data \
  -v /opt/openfang/config.toml:/data/config.toml \
  -e DEEPSEEK_API_KEY="sk-..." \
  --restart unless-stopped \
  openfang:0.6.4-offline
```

### 7.3 示例：切换到 Anthropic（Claude）

```bash
cat > /opt/openfang/config.toml << 'EOF'
[default_model]
provider = "anthropic"
model = "claude-sonnet-4-20250514"
api_key_env = "ANTHROPIC_API_KEY"

[memory]
decay_rate = 0.05
EOF

docker run -d \
  --name openfang \
  -p 4200:4200 \
  -v openfang-data:/data \
  -v /opt/openfang/config.toml:/data/config.toml \
  -e ANTHROPIC_API_KEY="sk-ant-..." \
  --restart unless-stopped \
  openfang:0.6.4-offline
```

### 7.4 示例：切换到 Groq

```bash
cat > /opt/openfang/config.toml << 'EOF'
[default_model]
provider = "groq"
model = "llama-3.3-70b-versatile"
api_key_env = "GROQ_API_KEY"

[memory]
decay_rate = 0.05
EOF

docker run -d \
  --name openfang \
  -p 4200:4200 \
  -v openfang-data:/data \
  -v /opt/openfang/config.toml:/data/config.toml \
  -e GROQ_API_KEY="gsk_..." \
  --restart unless-stopped \
  openfang:0.6.4-offline
```

### 7.5 示例：使用本地 Ollama（无需 API Key）

```bash
cat > /opt/openfang/config.toml << 'EOF'
[default_model]
provider = "ollama"
model = "qwen2.5:7b"
api_key_env = ""

[memory]
decay_rate = 0.05
EOF

docker run -d \
  --name openfang \
  -p 4200:4200 \
  -v openfang-data:/data \
  -v /opt/openfang/config.toml:/data/config.toml \
  --network host \
  --restart unless-stopped \
  openfang:0.6.4-offline
```

> Ollama 跑在宿主机上时，容器需要 `--network host` 才能访问宿主机的 `localhost:11434`。

### 7.6 示例：自建 OpenAI 兼容 API（内网代理/中转）

```bash
cat > /opt/openfang/config.toml << 'EOF'
[default_model]
provider = "openai"
model = "gpt-4o"
api_key_env = "MY_API_KEY"
base_url = "http://192.168.1.100:8000/v1"

[memory]
decay_rate = 0.05
EOF

docker run -d \
  --name openfang \
  -p 4200:4200 \
  -v openfang-data:/data \
  -v /opt/openfang/config.toml:/data/config.toml \
  -e MY_API_KEY="你的Key" \
  --restart unless-stopped \
  openfang:0.6.4-offline
```

> `base_url` 不要以 `/` 结尾，否则会拼出双斜杠导致请求失败。

## 八、注意事项

1. **端口冲突**：如果 4200 端口被占用，修改 `-p` 参数为其他端口，如 `-p 8080:4200`
2. **防火墙**：确保服务器防火墙放行了 4200 端口（`firewall-cmd --add-port=4200/tcp --permanent`）
3. **磁盘空间**：数据卷会随着使用增长，建议定期检查 `docker system df`
4. **API Key 安全**：生产环境建议使用 `--env-file` 而非命令行传参，避免 Key 出现在进程列表中

   ```bash
   # 创建环境文件
   cat > /opt/openfang/.env << 'EOF'
   OPENAI_API_KEY=你的智谱API Key
   EOF

   # 用 env-file 启动
   docker run -d \
     --name openfang \
     -p 4200:4200 \
     -v openfang-data:/data \
     --env-file /opt/openfang/.env \
     --restart unless-stopped \
     openfang:0.6.4-offline
   ```
